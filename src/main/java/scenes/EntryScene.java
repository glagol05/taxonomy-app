package scenes;

import controllers.Navigation;
import javafx.scene.Scene;

public class ViewEntryScene {
    public static Scene create(Navigation navigation) {
        Scene scene = new Scene(null);

        return scene;
    }
}
